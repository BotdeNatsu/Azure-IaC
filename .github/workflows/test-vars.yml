name: test

on:
  
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  test:
    runs-on:  ubuntu-latest
    environment:
      name: ${{ github.ref_name }}

    steps:

    # Deploy Custom Vision Model and publish
    - name: Test Vars
      uses: Azure/cli@v2
      with:
        inlineScript: |
          echo Prueba completa
          $varTest = ${{ vars.FUNCTION_VARIABLES}} | ConvertTo-Json
          echo $varTest.VISION_PREDICTION_ENDPOINT
          